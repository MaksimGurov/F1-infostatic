<div class="progress">
  <div class="progress-bar" role="progressbar" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100" style="width: 64%">Season progress : 64%</div>
</div>

<div class="container upcoming_races_table">
  <br>
  <h1>Upcoming races:</h1>
  <br>
  <table class="table table-striped table-hover table-bordered  table-dark  ">
    <thead>
    <tr>
      <th>#</th>
      <th>Race </th>
      <th>Date </th>
      <th>Location</th>
      <th>Country</th>
    </tr>
    </thead>

    <tbody >
    <% @upcoming_races.each do |element| %>

      <tr>
        <td><%= element[:raceID]%></td>
        <td><%= element[:raceName]%></td>
        <td><%= element[:raceDate]%></td>
        <td><%= element[:raceLocation]%></td>
        <td><%= location_flag(element[:raceCountry])%></td>
      </tr>

    <% end %>
    </tbody>
  </table>


  <br>


</div>





<div class="container">

  <h1>Past Races:</h1>
  <br>

  <table class="table table-striped table-hover table-bordered table-dark">
    <thead>
    <tr>
      <th>#</th>
      <th>Races </th>
      <th>Date </th>
      <th>Location </th>
      <th></th>
      <th>Results</th>
      <th>Race Wiki</th>
    </tr>
    </thead>

    <tbody>
    <% @finished_races.each do |element| %>
      <tr>
        <td><%= element[:raceID]%></td>
        <td><%= element[:raceName]%></td>
        <td><%= element[:raceDate]%></td>
        <td><%= element[:raceLocation]%></td>
        <td><%= location_flag(element[:raceCountry])%></td>
        <% if element[:raceURL] != nil && element[:raceStatus] == 'Finished' %>
          <td> <%= link_to "Race Results", results_path(:id =>  element[:raceID]) , :class => "btn btn-primary" %>  </td>
          <td> <%= link_to "Wiki", element[:raceURL]%>  </td>
      <% end %>
      </tr>
    <% end %>
    </tbody>
  </table>

</div>
